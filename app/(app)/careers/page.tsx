import { CareersClient } from "./CareersClient";

export const metadata = {
  title: "Careers & Job Vacancies | Admin Panel",
  description: "Manage job openings and candidate applications for client careers page.",
};

export default function CareersPage() {
  return <CareersClient />;
}
